You are playing PokéRogue through the `pokerogue` MCP tools (status, get_state, read_menu, select_option, press, start_run). Play ONE Classic run unattended until the party wipes, then stop and report. Nobody is watching; do not ask questions.

Procedure:
1. Call `status`. The game should be on the TITLE screen.
2. Call `start_run` with species ["Bulbasaur","Charmander","Squirtle"], slot 2, overwrite true.
3. Loop until a result has status "run_over" or "run_interrupted":
   - Every acting result carries `menu` (screen, option labels in cursor order, cursor, text). Decide from it; call `read_menu` only when you need the full option objects (e.g. shop costs, party HP) or the `cancel_effect`.
   - Act with `select_option` using a label EXACTLY as returned. There is no back button: to leave a screen, select the option that leaves it (Cancel, No).
   - COMMAND: choose Fight. FIGHT: choose the strongest damaging move with PP left (read_menu's `extra.moves` has pp/power). TARGET_SELECT: pick the enemy with the lowest HP.
   - PARTY/POST_BATTLE_SWITCH or PARTY/SWITCH: select Cancel. PARTY/FAINT_SWITCH: select a mon that is not fainted and not already active, then "Send Out". Never select Cancel on a must-answer party screen (read_menu's `screen_class` says must_answer).
   - MODIFIER_SELECT (the shop): take one free reward (row 1, cost 0) — prefer healing/Rare Candy/held items; if it opens a PARTY screen, pick a healthy mon and then Apply/Use/Teach. If a move needs replacing (SUMMARY/LEARN_MOVE or a confirm), you may decline with No / Cancel. If money allows (>= cost) you may also buy a Potion from the shop rows before taking the reward.
   - CONFIRM you don't understand: choose No. OPTION_SELECT (biome choice, etc.): choose the first option. Acknowledge-only screens with no options: `press` ACTION once. SUMMARY: `press` CANCEL.
   - status "timed_out": call `get_state` to keep waiting (it presses nothing). Never repeat a press because the last one seemed to fail.
   - status "stuck": the result carries `escape` with `untried` labels and a `ladder`. Choose an untried option first; otherwise the first unspent rung. Do not loop the same two screens more than twice.
   - Do NOT call `screenshot`. Do NOT call `start_run` again after the run ends.
4. When the run ends, print a short final report: last wave reached, roughly how many tool calls you made, every screen id you met that you did not understand, every tool error you received, and one sentence on which single extra tool would have saved you the most calls.
